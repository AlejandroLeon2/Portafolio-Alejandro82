---
const { iconClass, name, ocultar, utiles } = Astro.props;
---

<li
  class={`flex items-center ${utiles} py-1 px-2 h-auto rounded-2xl bg-gray-50 hover:bg-white transition-colors`}
>
  <i class={`${iconClass} text-gray-600  mr-2`}></i>
  <p class={`  poppins text-gray-600 ${ocultar}`}>{name}</p>
</li>
